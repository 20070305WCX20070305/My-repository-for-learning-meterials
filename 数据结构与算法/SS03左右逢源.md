# 左右逢源

## 解析树与抽象语法树 AST

1. 词法分析（Lexical Analysis）：此步骤中，输入的字符序列（代码、表达式等）被转换成一系列标记（tokens），例如数字、操作符、括号等。

2. 语法分析（Syntactic Analysis）：在该阶段，标记序列根据语法规则被组织成解析树。语法分析器会使用诸如递归下降、LL 解析、LR 解析等算法来构造树。

3. 语义分析（Semantic Analysis）：构建的解析树可能会进一步经过语义检查，以确保表达式的意义是合理的，比如类型检查等。


### 完全括号表达式

4种标记：左括号、右括号、运算符和操作数。

左括号代表新表达式的起点，所以应该创建一棵对应该表达式的新树。反之，遇到右括号则意味着到达该表达式的终点。操作数既是叶子节点，也是其运算符的子节点。此外，每个运算符都有左右子节点。

四条规则（对于一个运算过程模拟一下就行了，比较好理解）：

1. 如果当前标记是(，就为当前节点添加一个左子节点，并下沉至该子节点；

2. 如果当前标记在列表['+', '-', '/', '＊']中，就将当前节点的值设为当前标记对应的运算符；为当前节点添加一个右子节点，并下沉至该子节点；

3. 如果当前标记是数字，就将当前节点的值设为这个数并返回至父节点；

4.  如果当前标记是)，就跳到当前节点的父节点。

```python

class Stack:  # 定义栈（可以通用）
    def __init__(self):
        self.items = []
        self.stacksize = 0

    def is_empty(self):
        return self.stacksize == 0

    def push(self, newitem):
        self.items.append(newitem)
        self.stacksize += 1

    def pop(self):
        self.stacksize -= 1
        return self.items.pop()

    def peek(self):
        return self.items[self.stacksize - 1]

    def size(self):
        return self.stacksize

class BinaryTree: # 定义二叉树（可以通用）
    def __init__(self, rootObj):
        self.parnent = None
        self.key = rootObj
        self.left = None
        self.right = None

    def insertleft(self, newleft):
        if self.left is None:
            self.left = BinaryTree(newleft)
        else:
            t = BinaryTree(newleft)
            t.left = self.left
            self.left = t

    def insertright(self, newright):
        if self.right is None:
            self.right = BinaryTree(newright)
        else:
            t = BinaryTree(newright)
            t.right = self.right
            self.right = t
    
    def getRightChild(self):
        return self.rightChild

    def getLeftChild(self):
        return self.leftChild

    def setRootVal(self, obj):
        self.key = obj

    def getRootVal(self):
        return self.key

    def traversal(self, method=):
        if method == "preorder":
            print(self.key, end=" ")
        if self.leftChild != None:
            self.leftChild.traversal(method)
        if method == "inorder":
            print(self.key, end=" ")
        if self.rightChild != None:
            self.rightChild.traversal(method)
        if method == "postorder":
            print(self.key, end=" ")

def build_tree(fpxep): # 构建解析树，按照上面的四条准则，用栈来保存父节点
    fplist = fpexp.split()
    pstack = Stack()
    etree = BinaryTree(" ")
    pstack.push(etree)
    currenttree = etree

    for item in fdlist:
        if item == "(":
            currenttree.insertLeft(" ")
            pstack.push(currenttree)
            currenttree = curenttree.getLeftChild()
        elif item in "+-*/":
            currenttree.setRootVal(item)
            currenttree.insertRight(" ")
            pstack.push(currenttree)
            currenttree = cerrenttree.getRightChild
        elif item not in "()+-*/":
            currenttree.setRootVal(int(item))
            currenttree = pstack.pop()
        elif item == ")":
            currenttree = pstack.pop()

    return etree

```

如果要求和的话递推实现就行了，代码如下

```python

def calculation(etree):   # 函数名拼写也可以改为 calculation
    if isinstance(etree.key, int):   # 更稳健的类型检查
        return etree.key
    if etree.key == "+":
        return calculation(etree.getLeftChild()) + calculation(etree.getRightChild())
    if etree.key == "-":
        return calculation(etree.getLeftChild()) - calculation(etree.getRightChild())
    if etree.key == "*":
        return calculation(etree.getLeftChild()) * calculation(etree.getRightChild())
    if etree.key == "/":
        return calculation(etree.getLeftChild()) / calculation(etree.getRightChild())
    # 可选：处理未知操作符
    raise ValueError(f"Unknown operator: {etree.key}")
```

### 运算的中缀表达式转化为后缀表达式

##树的遍历与应用

外部函数与内部方法两种处理办法，如果用外部函数，不用检查是否为`None`，而内部方法必须检查：

```python
def preorder(self):
    print(self.key)
    if self.leftChild:
        self.leftChild.preorder()
    if self.rightChild:
        self.rightChild.preorder()

def preorder(tree):
    if tree:
        print(tree.getRootVal())
        preorder(tree.getLeftChild())
        preorder(tree.getRightChild())
```

### 调度场算法

两个结构：
- 输出队列
- 运算符栈：临时存放运算符，函数名与左括号

|Token类型   | 动作   |
|-----------|------------|
|数字/操作数|直接加入输出队列。|
|函数名  | 压入运算符栈（视为特殊的“一元前缀运算符”）。|
|逗号    | 依次弹出运算符栈顶的运算符并输出，直到遇到左括号。用于分隔函数参数。|
|运算符（+ - * / ^ 等）|当栈顶存在优先级更高或同级的运算符（结合性决定行为）时，弹出栈顶并输出；之后将当前运算符压栈。|
|左括号 ( |压入运算符栈。|
|右括号 ) |不断弹出栈顶运算符并输出，直到遇到左括号；弹出左括号但不输出。如果栈顶是函数名，也要弹出并输出（表示函数结束）。|

运算符优先级与结合性
|运算符|优先级|结合性|
|---|---|---|
|加减号  +-|1|左结合|
|乘除号  */|2|左结合|
|乘方  **|3|右结合|
|函数|最高|无|

- 左结合运算符（如 +）弹出时要求栈顶优先级 **大于或等于** 当前运算符；
- 右结合运算符（如 ^）弹出时要求栈顶优先级 **严格大于** 当前运算符。

#### 调度场算法的python实现

- 整数与浮点数
- 基本运算符：+、-、*、/、^（幂）
- 函数：sin、cos、max、min（用逗号分隔参数）
- 括号改变优先级
- 负数前缀（一元减号）的简单处理（通过将 - 识别为特殊一元运算符）

